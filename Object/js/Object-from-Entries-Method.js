var a = new Map([
    ['Danish','chouhan'],
    ['and age is',17]
]);
var b = Object.fromEntries(a);
console.log(b)

// in this you may see that we maked an array but if we want this in object this is the method in this every first paremeter will be a properties of object 
